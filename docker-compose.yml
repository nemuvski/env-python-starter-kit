version: '3'

services:
  devenv:
    container_name: devenv-python
    build:
      context: ./
    image: nemuvski/python-starter-kit:py3.6.4-alpine3.7
    volumes:
      - ./workspace:/home/dev/workspace
    command: sh
